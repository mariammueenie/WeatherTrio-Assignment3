<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main"
    android:orientation="vertical">

    <!--
        Displays required screen title at the top.
        A MaterialToolbar keeps title consistent with app theme.
    -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        app:title="@string/search_title"
        app:titleTextColor="@color/text_primary" />

    <!--
        Holds search controls and future city cards.
        The ScrollView allows complete city list to scroll.
    -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!--
                Allows the user to enter a city search.
                Filtering logic will be added after required cards exist.
            -->
            <EditText
                android:id="@+id/editSearchCity"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/search_hint"
                android:inputType="text"
                android:maxLines="1"
                android:padding="14dp"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary" />

            <!--
                Temporary message showing where required city cards will go.
                will be removed in the next step.
            -->
            <TextView
                android:id="@+id/textCityListPlaceholder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="@string/search_city_placeholder"
                android:textAppearance="@style/TextAppearance.WeatherTrio.Body" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>